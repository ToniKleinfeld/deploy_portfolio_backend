client_max_body_size 200M;  # allow larger uploads (video upload in admin)
proxy_read_timeout 300s;
proxy_send_timeout 300s;
proxy_connect_timeout 60s;

# Optional: simple rate limit to protect demo
# limit_req_zone $binary_remote_addr zone=req_zone:10m rate=5r/s;
# server { location / { limit_req zone=req_zone burst=10; } }